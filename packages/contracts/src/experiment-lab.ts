import { z } from "zod";
import { ArtifactFingerprintSchema, SemanticArtifactSchemaVersion, VersionedEntityReferenceSchema } from "./semantic-agent-artifacts.js";

const Id = z.string().min(3).max(240).regex(/^[a-z0-9][a-z0-9._:@-]*$/u);
const Timestamp = z.string().datetime({ offset: true });
const Ref = VersionedEntityReferenceSchema;
const IssueCodes = z.array(z.string().min(1).max(160)).max(32);
export const ExperimentObjectiveSchema = z.object({ kind: z.literal("maximize_total_return") }).strict();
export const ExperimentConstraintsSchema = z.object({ maxDrawdownPctLte: z.number().min(0).max(100).optional(), minimumTradeCount: z.number().int().min(0).max(1_000_000).optional(), walkForwardPositive: z.boolean().optional(), runtimeFailureCountEqZero: z.boolean().optional() }).strict();
export const ExperimentCreateRequestSchema = z.object({ schemaVersion: z.literal(SemanticArtifactSchemaVersion), idempotencyKey: z.string().min(8).max(160).regex(/^[A-Za-z0-9._:-]+$/u), participantVersionIds: z.array(Id).min(2).max(5), datasetId: Id, startAt: Timestamp, endAt: Timestamp, walkForwardPlanId: Id, comparisonMode: z.enum(["STRATEGY_COMPARISON", "MODEL_COMPARISON", "AGENT_GRAPH_COMPARISON", "OPEN_CLASS"]), objective: ExperimentObjectiveSchema, constraints: ExperimentConstraintsSchema }).strict().superRefine((v,c)=>{if(new Set(v.participantVersionIds).size!==v.participantVersionIds.length)c.addIssue({code:z.ZodIssueCode.custom,message:"participant_duplicate",path:["participantVersionIds"]});if(Date.parse(v.startAt)>=Date.parse(v.endAt))c.addIssue({code:z.ZodIssueCode.custom,message:"range_invalid",path:["endAt"]});});

export const ExperimentScorecardSchema = z.object({ totalReturnPct:z.number().finite(),maxDrawdownPct:z.number().finite().min(0),tradeCount:z.number().int().nonnegative(),fillCount:z.number().int().nonnegative(),riskRejectionCount:z.number().int().nonnegative(),cycleCount:z.number().int().positive(),equityPoints:z.array(z.object({asOf:Timestamp,equity:z.number().finite().nonnegative()}).strict()).min(1),unavailableMetrics:z.array(z.enum(["sharpe","sortino","profit_factor"])) }).strict();
export const ExperimentWalkForwardSchema = z.object({ foldCount:z.number().int().positive(),positiveValidation:z.boolean(),promotionEligible:z.boolean(),validationReturnsPct:z.array(z.number().finite()).min(1) }).strict();
export const ExperimentEvidenceSchema = z.object({ evidenceRef:z.string().min(1).max(500),artifactId:Id,artifactFingerprint:ArtifactFingerprintSchema,resultFingerprint:ArtifactFingerprintSchema,manifestFingerprint:ArtifactFingerprintSchema,promotionEligible:z.boolean(),scorecard:ExperimentScorecardSchema.optional(),walkForward:ExperimentWalkForwardSchema.optional(),lineage:z.object({planRef:Ref,datasetRef:Ref,profileScopeRef:Ref}).strict() }).strict();
export const ExperimentConstraintResultSchema = z.object({ key:z.enum(["maxDrawdownPctLte","minimumTradeCount","walkForwardPositive","runtimeFailureCountEqZero"]),passed:z.boolean(),actual:z.union([z.number().finite(),z.boolean()]),expected:z.union([z.number().finite(),z.boolean()]) }).strict();
export const ExperimentCandidateSchema = z.object({candidateId:Id,status:z.literal("candidate_for_validation"),fingerprint:ArtifactFingerprintSchema,participantId:Id,experimentFingerprint:ArtifactFingerprintSchema,evidenceFingerprint:ArtifactFingerprintSchema,constraintResults:z.array(ExperimentConstraintResultSchema),runtimeApplied:z.literal(false)}).strict();
export const ExperimentReplaySchema = z.object({ replayId:Id,definitionFingerprint:ArtifactFingerprintSchema,evidenceFingerprint:ArtifactFingerprintSchema,resultFingerprint:ArtifactFingerprintSchema,status:z.literal("verified"),issueCodes:IssueCodes }).strict();
export const ExperimentParticipantSchema = z.object({participantId:Id,label:z.string().min(1).max(120),strategyVersionRef:Ref,strategyFingerprint:ArtifactFingerprintSchema,executableFingerprint:ArtifactFingerprintSchema,profileRef:Ref,candidateSetRef:Ref,sourceRefs:z.array(Ref).min(1).max(32),configProjection:z.object({marketPackId:z.string().min(1),modelMode:z.enum(["none","rule"]),executionModel:z.literal("graph_trading"),riskFingerprint:ArtifactFingerprintSchema,graphFingerprint:ArtifactFingerprintSchema}).strict(),backtestJobId:Id.optional(),walkForwardJobId:Id.optional(),backtestEvidence:ExperimentEvidenceSchema.optional(),walkForwardEvidence:ExperimentEvidenceSchema.optional(),constraintResults:z.array(ExperimentConstraintResultSchema),issueCodes:IssueCodes}).strict();
export const ExperimentSchema = z.object({schemaVersion:z.literal(SemanticArtifactSchemaVersion),experimentId:Id,fingerprint:ArtifactFingerprintSchema,createdAt:Timestamp,actorId:Id,lifecycleStatus:z.enum(["draft","backtest_partial","backtest_complete","walk_forward_partial","evidence_complete","candidate_ready","insufficient","stale","failed"]),comparability:z.object({status:z.enum(["CONTROLLED","OPEN_CLASS","INCOMPATIBLE"]),requestedMode:z.string(),changedDimensions:z.array(z.string()).max(16),lockedDimensions:z.array(z.string()).min(1).max(32),issueCodes:IssueCodes}).strict(),lock:z.object({datasetRef:Ref,startAt:Timestamp,endAt:Timestamp,walkForwardPlanRef:Ref,objective:ExperimentObjectiveSchema,constraints:ExperimentConstraintsSchema,executionModel:z.literal("graph_trading"),riskLockFingerprint:ArtifactFingerprintSchema,modelMode:z.enum(["none","rule"]),failurePolicy:z.literal("fail_closed"),runtimeApplied:z.literal(false),exchangeWriteAllowed:z.literal(false)}).strict(),participants:z.array(ExperimentParticipantSchema).min(2).max(5),candidate:ExperimentCandidateSchema.optional(),replay:ExperimentReplaySchema.optional()}).strict();
export type ExperimentCreateRequest=z.infer<typeof ExperimentCreateRequestSchema>; export type Experiment=z.infer<typeof ExperimentSchema>;
